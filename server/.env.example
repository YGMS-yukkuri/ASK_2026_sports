# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ask_2026_sports
DB_USER=postgres
DB_PASSWORD=changeme

# Admin Configuration
ADMIN_PASSWORD=changeme

# CORS Configuration
# Dev: http://localhost:5173 (Vite). Prod: the public origin, e.g. https://sports.stemask.com
CORS_ORIGIN=https://sports.stemask.com

# Image Upload
MAX_FILE_SIZE=20971520
UPLOAD_DIR=./data/images

# Optional Redis (falls back to in-memory cache if absent)
# REDIS_URL=redis://localhost:6379
