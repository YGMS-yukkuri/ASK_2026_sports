# これは、バグなどの問題点をまとめたものです。
## この中にアイテムがある場合は、修正する必要があります。
- OFFSET ページング: 投稿増加で劣化
- Redis キャッシュなし: 高負荷時DB負荷増大
- 画像(img)が読み込まれるまでページがロード完了しない
    - 初めはすべてフォールバックとし、画像が読み込まれたら置き換える
    - 画像は読み込みが完了するまで、フォールバックとしてロード中であることを示すアニメーションを表示する
    - アニメーションはcssを用いた簡易的なものでよい